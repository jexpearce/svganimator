{
  "name": "@motif/schema",
  "version": "0.1.0",
  "description": "Shared TypeScript types and interfaces for Motif",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "test": "vitest"
  },
  "devDependencies": {
    "typescript": "^5.0.0",
    "vitest": "^1.0.0"
  }
} 